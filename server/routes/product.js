const express = require('express');
const router = express.Router();


//=================================
//             Product
//=================================

// router.app.post ('api/product/image', 값)
router.app.post ('/image', (req, res) => {
    // 가져온 이미지를 저장을 해주면 된다.
})

module.exports = router;
